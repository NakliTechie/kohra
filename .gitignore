# Local working notes — never committed
plan/

# Machine-local launch config (absolute paths) — never committed
.claude/

# External reference clones — never committed
vendor/

# OS junk
.DS_Store
Thumbs.db

# Python
__pycache__/
*.pyc
.venv/

# Node
node_modules/

# Model artifacts (large; published to HF, not git)
*.onnx
*.onnx_data
models/
